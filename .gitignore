# Listes brutes : pur cache, regenerable en ~20 s via fetch_filters.py.
filters/*.txt

# Sorties : publiees sur la branche orpheline `dist` par la CI, pas sur `main`.
webkit-rules/*.json
webkit-rules/extended/*.json

*.tmp
.DS_Store
__pycache__/
